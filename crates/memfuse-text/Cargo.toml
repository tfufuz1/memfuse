[package]
name = "memfuse-text"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "MemFuse — Text processing and BM25 search for Hybrid Search"

[dependencies]
memfuse-core = { workspace = true }
itoa = { workspace = true }
unicode-segmentation = "1.11.0"
async-trait.workspace = true
bincode = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
parking_lot = { workspace = true }

[dev-dependencies]
tokio = { workspace = true, features = ["test-util", "macros"] }
tempfile = { workspace = true }
memfuse-store = { version = "0.1.0", path = "../memfuse-store" }

# ANCHOR:ARCH:DAG-001 — Architektur-Layer durchbrochen
# WP:NONE PRIO:1 NEEDS:NONE
# AGENT:05 DATE:2026-05-25 STATUS:DONE
# CREATED:2026-05-09 DEADLINE:NONE
# VERLETZUNG: memfuse-text darf memfuse-store nicht direkt importieren
# AKTION: memfuse-store wurde aus [dependencies] entfernt und ist nur noch
#         als [dev-dependencies] für Integrationstests erlaubt.
#         Produktionscode nutzt ausschließlich die Storage-Abstraktion in core.
