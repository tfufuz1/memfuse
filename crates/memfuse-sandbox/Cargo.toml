[package]
name = "memfuse-sandbox"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "WASM sandbox for secure agent tool execution in MemFuse"

[dependencies]
async-trait.workspace = true
memfuse-core = { version = "0.1.0", path = "../memfuse-core" }
wasmtime = "20.0"

[dev-dependencies]
tokio = { workspace = true }
wat = "1.0"
